*This Project is to enhance basic skills. 
*Using only basic HTML, CSS and JS. 
*No extra tech or framework or library is used.

*working with 3rd party API using fetch to make an HTTP request.

*fetch: it's built into the browser, a Native API, so we don't have 
	to include any kind of CDN or anything.

*Working with JSON API, a Javascript Object.

*using async-await rather than .then() method, while using promises.

*Learn to use Libraries like Axios for making HTTP request, because its eaiser.

https://icanhazdadjoke.com/api

